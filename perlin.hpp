#ifndef PERLIN_HPP
#define PERLIN_HPP

// TODO: octaves, random gen

namespace perlin {

template <typename T>
inline T perlin_3d(T x, T y, T z, T t = 0) {
    return 0.0;
}

template <typename T>
inline T perlin_2d(T x, T y, T t = 0) {
    return 0.0;
}

template <typename T>
inline T perlin_1d(T x, T t = 0) {
    return 0.0;
}

}  // namespace perlin

#endif
